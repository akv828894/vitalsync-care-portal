# End-of-Day Tests

Send me your answers after each day and I can test you properly from these.

## Day 1 Test

1. Why do we hash passwords before saving them to MongoDB?
2. What is the difference between a route file and a controller file?
3. What does the `User` model store in this project?
4. Why is `email` converted to lowercase before saving?
5. What is the job of `connectDatabase()`?

## Day 2 Test

1. What is a JWT in simple words?
2. Where is the JWT generated in this project?
3. What is inside the `Authorization` header?
4. What happens inside `protect` middleware?
5. What status code should we return when login credentials are wrong?

## Day 3 Test

1. What happens from clicking the Register button to saving a user in MongoDB?
2. Why do we keep token and user in frontend auth state?
3. Why is password never stored in Zustand or localStorage?
4. What does `apiRequest()` do?
5. Why should we show backend error messages on the form?

## Day 4 Test

1. How does the frontend know whether to allow `/dashboard`?
2. What is the role of `ProtectedRoute`?
3. Why do we fetch `/api/auth/me` after reloading the page?
4. What happens when a token is expired?
5. What is the difference between authentication and authorization?

## Day 5 Test

1. Why do CORS errors appear mostly after deployment?
2. Which environment variables are needed on Render?
3. Which environment variables are needed on Vercel?
4. What exact flow will you show in your demo video?
5. If the interviewer asks "why localStorage and not HttpOnly cookies?", what will you answer?

## Self-Check Rule

If you cannot answer at least `4 out of 5` questions for the day without looking at code, revise that day before moving forward.
